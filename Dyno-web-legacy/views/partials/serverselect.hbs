<span class="select">
	<select class="level-item server-select">
		<option>Select Server</option>
		{{#each guilds}}
			<option value="{{this.id}}"{{#if this.selected}} selected="selected"{{/if}}>{{this.name}}</option>
		{{/each}}
	</select>
</span>